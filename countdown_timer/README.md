# Countdown Timer

A simple Python countdown timer that counts down from a given number of seconds.

## Features
- Displays time in HH:MM:SS format
- Uses time module
- Updates every second

## How to run
python countdown.py